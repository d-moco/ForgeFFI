[package]
name = "forgeffi-net-ffi"
version = "0.1.0"
edition = "2024"

[dependencies]
forgeffi-net = { path = "../forgeffi-net" }
forgeffi-base = { path = "../forgeffi-base" }

[lib]
path = "src/lib.rs"
crate-type = ["cdylib", "staticlib"]

